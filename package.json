{
  "name": "captcha-sast-benchmark",
  "version": "0.1.0",
  "private": true,
  "description": "Intentionally vulnerable localhost-only CAPTCHA verification benchmark for SAST evaluation.",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.9",
    "@types/express": "^5.0.3",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "@types/node": "^24.5.2"
  },
  "engines": {
    "node": ">=20"
  }
}
